version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: obsidian-obsidian_1
      APP_PORT: 8080
    
  server:
    image: hedgepay/obsidian:latest
    user: "1000:1000"
    init: true
    volumes:
      - ./vault:/vault
    ports:
      - "8080:8080"
    restart: unless-stopped
